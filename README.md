In the previous part, we created the foundation of our multimodal RAG application using ASP.NET Core.

We created the MVC project, built the upload form, created the RAG controller, and successfully uploaded document and image files.

## Code Versions by Video

Each part of the tutorial series is available as a Git tag:

- Part 1 – Upload Form: `part-1-upload-form`
- Part 2 – Text Extraction: `part-2-text-extraction`
- Part 3 – Text Chunking: `part-3-text-chunking`
- Part 4 – Vector Embeddings: `part-4-vector-embeddings`

To download a specific version:

```bash
git checkout part-4-vector-embeddings

